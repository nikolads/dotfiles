#
# ~/.bashrc
#
# Sourced when bash is invoked as interactive non-login shell
#

# If not running interactively, don't do anything
[[ $- != *i* ]] && return

alias ls='ls --color=auto'
PS1='[\u@\h \W]\$ '
